; backend/docker/php/app.ini — baseline shared across all stages

[PHP]
memory_limit        = 256M
max_execution_time  = 60
upload_max_filesize = 32M
post_max_size       = 32M
log_errors          = On

[Date]
date.timezone = UTC
